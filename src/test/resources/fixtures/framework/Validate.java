package ca.bnc.lsist.core.utils;

/** Fixture stand-in for the lsist Validate (with nested Objects) — parsed by FrameworkApiExtractor, not compiled. */
public class Validate {

    public static class Objects {
        public static void isNotNull(Object value, String context) {
        }

        public static void areEqual(Object expected, Object actual, String context) {
        }
    }
}
